# n8n-local

Docker Compose for running n8n locally on port 5678. Workflow data persists in `./n8n_data/` (gitignored).

```bash
docker compose up -d
# open http://localhost:5678
docker compose down
```
